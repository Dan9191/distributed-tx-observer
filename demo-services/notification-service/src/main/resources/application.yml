server:
  port: 8085

management:
  endpoints:
    web:
      exposure:
        include: prometheus,health

spring:
  application:
    name: notification-service

# Настройки регистрации шагов в notification-service.
notification:
  service-url: ${OBSERVER_SERVICE_URL:http://localhost:8033}
  service-name: notification-service
  transactions:
    - name: CreateOrder
      steps:
        - SendNotification
